Примечания:
- массив размером от 1 до 10000
- шаг измерений - 500
- можно использовать matplotlib

1) Задача №5 ЛР №2:
- операция индексации a[i]
- формальная замена на *(a + i)
- использование указателей
--------------------------------------------------------------------------------

2) Прогнать на уровнях оптимизации Os, O0, O1, O2, O3

3) Скрипты:
build_apps.sh - компиляция, получение 3 * 5 исполняемых файлов.

update_data.sh - добавить данные в датасет экспериментов.

make_preproc.sh - посчитать среднее арифметическое, медианное, найти максимум и минимум, вычислить нижний и верхний квартили. Результаты записать для последующего вывода.

make_postptoc.py - получить графики.

go.sh - вызвать все скрипты, получить данные эксперимента.




ОТЧЁТ
1) Графики:
- зависимость времени от числа элементов (x15)
- для всех О2 график с ошибкой (среднее, максимум, минимум)
- для О3 и a[i] график с усами (среднее, максимум, минимум, нижний, средний и верхний квартили)

2) Таблицы:
- таблицы ко всем указанным графикам
- таблица для О2 и a[i] со столбцами: длина массива n, время выполнения t(n), величина для всех строк, кроме последней
(ln(𝑡𝑖+1) − ln(𝑡𝑖)) / (ln(𝑛𝑖+1) − ln(𝑛𝑖))

3) Ответить на вопросы:
- какой способ обработки быстрее и почему
