#ifndef __INTERFACE__H__
#define __INTERFACE__H__

#include <stdio.h>

int print(char *filename);
int sort(char *filename);
int create(char *filename, size_t count);

#endif // #ifndef __INTERFACE__H__
